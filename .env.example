# --- Database (Docker Compose) ---
POSTGRES_USER=library
POSTGRES_PASSWORD=changeme
POSTGRES_DB=library
DATABASE_URL=postgresql://library:changeme@localhost:5434/library

# --- AI Gateway (Vercel) ---
# We route all model calls through Vercel AI Gateway (DECISIONS.md).
# Get a key at: https://vercel.com/[team]/~/ai-gateway/api-keys
# Default model is google/gemini-3.5-flash; swap via lib/ai/index.ts.
AI_GATEWAY_API_KEY=

# --- Budget enforcement ---
# Hard daily cap (USD). Pipeline pauses when reached.
DAILY_BUDGET_USD=20

# --- Pipeline paths ---
WIKIDATA_DUMP_PATH=./dumps/latest-all.json.bz2
WIKIPEDIA_DUMP_PATH=./dumps/enwiki-latest-pages-articles-multistream.xml.bz2

# --- Wikimedia compliance ---
# Per Wikimedia API policy, requests MUST include a descriptive User-Agent
# with project identifier and a contact email/URL.
WIKIMEDIA_USER_AGENT=Alexandria/0.1 (local development; contact: andrestran@icloud.com)
